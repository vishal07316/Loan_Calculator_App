import React from 'react';
import { Typography } from '@mui/material';

const CurrencyConverter = () => (
  <div>
    <Typography variant="h5">CurrencyConverter</Typography>
  </div>
);

export default CurrencyConverter;