import React from 'react';
import { Typography } from '@mui/material';

const ThemeToggle = () => (
  <div>
    <Typography variant="h5">ThemeToggle</Typography>
  </div>
);

export default ThemeToggle;