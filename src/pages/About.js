import React from 'react';
import { Typography, Box } from '@mui/material';

const About = () => (
  <Box sx={{ p: 3 }}>
    <Typography variant="h4">About This App</Typography>
    <Typography>
      The Loan Calculator App is a modern, single-page web application built using React JS and Material UI...
      {/* Add the rest of the content from the assignment */}
    </Typography>
  </Box>
);

export default About;